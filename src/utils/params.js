const product = {
	"0":"启用"
}

/*
 用途：检查输入字符串是否是手机号格式不带国家区号
 输入：str：字符串
 返回：如果通过验证返回true,否则返回false
 */
function getDisplay(appName, appValue) {
	return(appName.appValue);
}
export {
    getDisplay
}